# Puerto del servidor relay (salas)
PORT=3000

# URL del relay que usara el cliente para el modo sala (opcional).
# Si se omite, el cliente funciona en modo SOLO (sin servidor).
CATUNES_RELAY=ws://localhost:3000
